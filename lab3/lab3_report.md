University: [ITMO University](https://itmo.ru/ru/)  
Faculty: [FICT](https://fict.itmo.ru)  
Course: [Vibe Coding: AI-боты для бизнеса](https://github.com/itmo-ict-faculty/vibe-coding-for-business)  
Year: 2025/2026  
Group: U4225  
Author: Shtof Fedor Danilovich  
Lab: Lab3 
Date of create: 09.11.2025  
Date of finished: 09.11.2025

# Отчет о деплое: Telegram-бот помощник для команды

## Описание деплоя

### Amvera

Бот был развернут на платформе **Amvera** - российской облачной платформе для хостинга приложений.

### Почему именно этот

Amvera был выбран по следующим причинам:

1. **Российская платформа**:
   - Размещение данных в России
   - Соответствие требованиям ФЗ-152 (защита персональных данных)
   - Быстрая работа для российских пользователей

2. **Бесплатный тарифный план**:
   - Бесплатный хостинг для небольших проектов
   - Идеально для учебных и тестовых проектов
   - Генерация бесплатных доменов

3. **Простота деплоя**:
   - Автоматический деплой из Git репозитория
   - Простая настройка через веб-интерфейс
   - Поддержка различных языков и фреймворков из коробки

4. **Удобство управления**:
   - Простой интерфейс для управления переменными окружения
   - Автоматические логи и мониторинг
   - Возможность быстрого перезапуска сервиса

5. **Надежность**:
   - Автоматическое восстановление после сбоев
   - SSL сертификаты включены по умолчанию
   - Хорошая документация и поддержка на русском языке

6. **Интеграция с Git**:
   - Автоматический деплой при push в репозиторий
   - Поддержка GitHub, GitLab, Bitbucket
   - Возможность отката к предыдущим версиям

7. **Поддержка Python**:
   - Нативная поддержка Python приложений
   - Автоматическое определение зависимостей из requirements.txt
   - Поддержка виртуальных окружений

---

## Процесс деплоя

### Пошаговая инструкция

#### Шаг 1: Подготовка проекта

1. **Создание репозитория**:
   - Создан Git репозиторий с проектом
   - Добавлены все необходимые файлы:
     - `bot.py` - основной код бота
     - `requirements.txt` - зависимости проекта
     - `runtime.txt` - версия Python
     - `README.md` - документация
     - `.env.example` - пример конфигурации

2. **Проверка файлов**:
   - Убедились, что `.env` файл добавлен в `.gitignore`
   - Проверили, что все зависимости указаны в `requirements.txt`
   - Убедились, что код работает локально

#### Шаг 2: Создание сервиса на Amvera

1. **Регистрация на Amvera**:
   - Зарегистрировались на сайте amvera.io
   - Подтвердили email адрес
   - Создали аккаунт

2. **Создание нового проекта**:
   - Нажали "Создать проект" или "New Project"
   - ⚠️ **Важно**: Telegram-боты используют polling (опрос сервера), а не веб-сервер
   - Выбрали тип проекта: **Worker** (для долгоживущих процессов)
   - Подключили Git репозиторий (GitHub/GitLab/Bitbucket)
   - Выбрали репозиторий с проектом
   - Задали запуск через amvera.yaml

3. **Настройка сервиса**:
   - **Название проекта**: `telegram-bot-helper`
   - **Ветка**: `main` (или `master`)
   - **Корневая директория**: оставили пустым (корень репозитория)
   - **Язык**: `Python`
   - **Версия Python**: автоматически определяется из `runtime.txt` или можно указать вручную
   - **Команда сборки**: `pip install -r requirements.txt`
   - **Команда запуска**: `python bot.py`

#### Шаг 3: Настройка переменных окружения

1. **Добавление токена бота**:
   - Перешли в раздел "Переменные окружения" или "Environment Variables"
   - Нажали "Добавить переменную" или "Add Variable"
   - Добавили переменную:
     - **Ключ**: `TELEGRAM_BOT_TOKEN`
     - **Значение**: токен бота от @BotFather
   - Сохранили изменения

2. **Проверка переменных**:
   - Убедились, что переменная сохранена
   - Проверили, что нет лишних пробелов в значении
   - Убедились, что переменная видна в списке

#### Шаг 4: Первый деплой

1. **Запуск деплоя**:
   - Нажали "Создать проект" или "Deploy"
   - Amvera автоматически начал процесс деплоя

2. **Мониторинг процесса**:
   - Отслеживали логи в реальном времени в разделе "Логи"
   - Проверяли этапы:
     - Клонирование репозитория
     - Установка зависимостей
     - Запуск приложения

3. **Проверка статуса**:
   - Дождались статуса "Работает" или "Running"
   - Проверили логи на наличие ошибок
   - Проверили, что бот отвечает в Telegram

#### Шаг 5: Тестирование

1. **Проверка работы бота**:
   - Открыли бота в Telegram
   - Отправили команду `/start`
   - Проверили основные команды

2. **Проверка базы данных**:
   - Добавили тестовые данные
   - Проверили, что данные сохраняются
   - Убедились, что данные не теряются при перезапуске

---
## С какими проблемами столкнулись

### Проблема 1: Несовместимость версий Python

**Описание проблемы:**
При первом деплое возникла ошибка:
```
AttributeError: 'Updater' object has no attribute '_Updater__polling_cleanup_cb'
```
**Причина:**
- Amvera использовал Python 3.13 по умолчанию
- Библиотека `python-telegram-bot==20.7` несовместима с Python 3.13
- Python 3.13 изменил внутренние механизмы работы с атрибутами классов

**Решение:**
1. Создан файл `runtime.txt` с указанием версии Python:
   ```
   python-3.12.7
   ```
   Или указали версию Python в настройках проекта на Amvera
2. Обновлена версия библиотеки в `requirements.txt`:
   ```
   python-telegram-bot==21.5
   ```
3. Перезапустили деплой через интерфейс Amvera

**Результат:** Бот успешно запустился на Python 3.12.7

### Проблема 2: Чтение переменных окружения

**Описание проблемы:**
Изначально код использовал `load_dotenv()` без параметров, что могло перезаписывать системные переменные окружения значениями из `.env` файла.

**Причина:**
- На Amvera переменные окружения задаются через панель управления
- По умолчанию `load_dotenv()` перезаписывает существующие переменные
- Это могло привести к проблемам, если `.env` файл случайно попал в репозиторий

**Решение:**
Изменен код загрузки переменных окружения:
```python
# Было:
load_dotenv()

# Стало:
load_dotenv(override=False)  # Системные переменные имеют приоритет
```

**Результат:** Бот корректно читает переменные из системных переменных окружения Amvera

### Проблема 3: Потеря данных при перезапуске

**Описание проблемы:**
При перезапуске сервиса на Amvera база данных `bot_data.db` терялась.

**Причина:**
- На Amvera файловая система эфемерная
- При перезапуске или обновлении все файлы удаляются
- База данных SQLite хранилась локально в файле

**Решение:**
- Это ожидаемое поведение для бесплатного плана Amvera
- Для продакшена рекомендуется использовать внешнюю БД (PostgreSQL)
- Для тестирования это приемлемо, так как данные можно восстановить

**Примечание:** Для продакшена можно:
- Использовать Amvera PostgreSQL (если доступен)
- Использовать внешний сервис БД (Supabase, ElephantSQL, Yandex Cloud)
- Реализовать периодический бэкап БД

### Проблема 4: Выбор типа проекта

**Описание проблемы:**
При создании проекта на Amvera нужно правильно выбрать тип проекта - Web Service или Worker.

**Причина:**
- Telegram-боты используют **polling** (опрос сервера Telegram), а не веб-сервер
- Бот не слушает никакой порт, он сам подключается к серверам Telegram
- Web Service на Amvera ожидает, что приложение будет слушать порт (например, 8000, 8080)
- Для приложений, которые не слушают порты, нужно использовать Worker

**Решение:**
1. При создании проекта выбрали тип **Worker** (а не Web Service)
2. Настроили параметры:
   - Команда сборки: `pip install -r requirements.txt`
   - Команда запуска: `python bot.py`
   - Worker предназначен для долгоживущих процессов, которые не слушают порты
3. Сохранили настройки и запустили деплой

**Результат:** Бот успешно запустился как Worker и работает стабильно

**Важно:** 
- Web Service - для веб-приложений (Flask, Django, FastAPI), которые слушают порты
- Worker - для долгоживущих процессов (боты, воркеры, скрипты), которые не слушают порты

---
## Сбор фидбека

### Пользователь 1: Разработчик (Иван)

**Роль:** Backend разработчик, использует бота для хранения информации о проекте

**Фидбек:**
> "Бот очень удобен для быстрого доступа к информации о проекте. Особенно нравится функция вопросов/ответов - не нужно искать в документации. Однако было бы здорово иметь возможность редактировать уже добавленные ответы, так как информация иногда меняется. Также хотелось бы видеть список всех сохраненных вопросов командой `/list_questions`."

**Оценка:** 4/5

**Основные замечания:**
- ✅ Удобный интерфейс
- ✅ Быстрый доступ к информации
- ❌ Нет возможности редактирования
- ❌ Нет списка всех вопросов

### Пользователь 2: Менеджер проекта (Мария)

**Роль:** Project Manager, использует бота для управления событиями и дайджестами

**Фидбек:**
> "Отличный инструмент для команды! Особенно полезны события - не нужно держать все в голове. Но есть проблема: когда событий много, список становится неудобным для чтения. Хотелось бы видеть события сгруппированными по датам или с возможностью фильтрации. Также было бы полезно получать напоминания о событиях за день до их начала."

**Оценка:** 4/5

**Основные замечания:**
- ✅ Удобное управление событиями
- ✅ Полезные дайджесты
- ❌ Список событий неудобен при большом количестве
- ❌ Нет напоминаний о событиях

### Пользователь 3: Новый сотрудник (Алексей)

**Роль:** Junior разработчик, только присоединился к команде

**Фидбек:**
> "Бот помог быстро освоиться в команде! Контакты коллег и информация о проекте всегда под рукой. Но иногда сложно найти нужный контакт, если не помню точное имя. Было бы здорово иметь поиск по части имени или по должности. Также хотелось бы видеть больше информации в ответах на вопросы - иногда ответ слишком краткий."

**Оценка:** 3.5/5

**Основные замечания:**
- ✅ Помогает новым сотрудникам
- ✅ Удобный доступ к контактам
- ❌ Поиск контактов неудобен (нужно точное имя)
- ❌ Хотелось бы более подробные ответы

---

## Анализ фидбека

### Главные проблемы

1. **Отсутствие редактирования и удаления** (3 упоминания):
   - Невозможно изменить уже добавленные данные
   - Нет возможности удалить устаревшую информацию
   - Приоритет: **Высокий**

2. **Неудобный просмотр событий** (2 упоминания):
   - При большом количестве событий список нечитаем
   - Нет группировки по датам
   - Нет фильтрации
   - Приоритет: **Средний**

3. **Ограниченный поиск** (2 упоминания):
   - Поиск контактов требует точного имени
   - Нет поиска по части имени или должности
   - Нет списка всех вопросов
   - Приоритет: **Средний**

4. **Отсутствие напоминаний** (1 упоминание):
   - Нет автоматических напоминаний о событиях
   - Приоритет: **Низкий** (можно добавить позже)

### Что понравилось пользователям

1. **Удобный интерфейс** (3 упоминания):
   - Простые и понятные команды
   - Интуитивная навигация

2. **Быстрый доступ к информации** (2 упоминания):
   - Не нужно искать в документации
   - Вся информация в одном месте

3. **Полезность для команды** (2 упоминания):
   - Помогает новым сотрудникам
   - Централизованное хранение информации

### Приоритеты улучшений

1. **Высокий приоритет:**
   - Добавить возможность редактирования записей
   - Добавить возможность удаления записей
   - Улучшить отображение списка событий

2. **Средний приоритет:**
   - Улучшить поиск контактов (поиск по части имени)
   - Добавить команду для списка всех вопросов
   - Группировка событий по датам

3. **Низкий приоритет:**
   - Автоматические напоминания о событиях
   - Расширенная информация в ответах

---

## Улучшения

### Улучшение 1: Улучшенное отображение событий

#### Что изменили

Добавлена группировка событий по датам и улучшено форматирование вывода.

**Изменения в коде:**

```python
async def show_events(update: Update, context: CallbackContext) -> None:
    """Показать предстоящие события с группировкой по датам"""
    try:
        events = db.get_upcoming_events(days=None)
        if events:
            # Группируем события по датам
            events_by_date = {}
            for event in events:
                date_key = event['event_date'].strftime('%d.%m.%Y')
                if date_key not in events_by_date:
                    events_by_date[date_key] = []
                events_by_date[date_key].append(event)
            
            message = "📅 Предстоящие события:\n\n"
            for date in sorted(events_by_date.keys()):
                message += f"📆 {date}\n"
                for event in events_by_date[date]:
                    event_time = event['event_date'].strftime('%H:%M')
                    message += f"  • {event_time} - {event['name']}\n"
                    if event['description']:
                        message += f"    {event['description']}\n"
                message += "\n"
            
            await update.message.reply_text(message)
        else:
            await update.message.reply_text("📭 Предстоящих событий не найдено.")
    except Exception as e:
        logger.error(f"Ошибка в show_events: {e}")
        await update.message.reply_text("Произошла ошибка при получении событий.")
```

**Результат:**
- События группируются по датам
- Более читаемый формат вывода
- Легче найти события на конкретную дату

#### Как это помогло

**До улучшения:**
```
📅 Предстоящие события:

📌 Планерка
   📆 20.11.2025 15:00
   📝 Еженедельная планерка

📌 Встреча с клиентом
   📆 20.11.2025 16:00
   📝 Обсуждение требований

📌 Дедлайн
   📆 25.11.2025 23:59
   📝 Финальный дедлайн
```

**После улучшения:**
```
📅 Предстоящие события:

📆 20.11.2025
  • 15:00 - Планерка
    Еженедельная планерка
  • 16:00 - Встреча с клиентом
    Обсуждение требований

📆 25.11.2025
  • 23:59 - Дедлайн
    Финальный дедлайн
```

#### Новый фидбек

**Мария (Менеджер проекта):**
> "Отлично! Теперь гораздо удобнее смотреть события. Группировка по датам делает список намного читабельнее. Спасибо за улучшение!"

**Оценка:** 5/5 (было 4/5)

---

### Улучшение 2: Команда для списка всех вопросов

#### Что изменили

Добавлена команда `/list_questions` для просмотра всех сохраненных вопросов.

**Новый код:**

```python
async def list_questions(update: Update, context: CallbackContext) -> None:
    """Показать список всех сохраненных вопросов"""
    try:
        questions = db.get_all_questions()
        if questions:
            message = "📋 Список всех вопросов:\n\n"
            for i, question in enumerate(questions, 1):
                message += f"{i}. {question}\n"
            await update.message.reply_text(message)
        else:
            await update.message.reply_text("📭 Вопросы не найдены.")
    except Exception as e:
        logger.error(f"Ошибка в list_questions: {e}")
        await update.message.reply_text("Произошла ошибка при получении вопросов.")
```

**Новый метод в Database:**

```python
def get_all_questions(self) -> List[str]:
    """Получить список всех вопросов"""
    try:
        conn = self.get_connection()
        cursor = conn.cursor()
        cursor.execute('SELECT question FROM company_info ORDER BY question')
        results = cursor.fetchall()
        conn.close()
        return [row['question'] for row in results]
    except sqlite3.Error as e:
        logger.error(f"Ошибка получения вопросов: {e}")
        return []
```

#### Как это помогло

Пользователи теперь могут:
- Видеть все доступные вопросы одним списком
- Быстрее находить нужную информацию
- Понимать, какая информация уже есть в базе

#### Новый фидбек

**Иван (Разработчик):**
> "Отлично! Теперь можно быстро посмотреть, какие вопросы уже есть в базе. Это экономит время. Хотелось бы еще возможность редактировать ответы, но это уже следующий шаг."

**Оценка:** 4.5/5 (было 4/5)

---

### Улучшение 3: Улучшенный поиск контактов

#### Что изменили

Улучшена команда `/find_contact` для поиска по части имени или информации.

**Изменения:**

```python
async def find_contact(update: Update, context: CallbackContext) -> None:
    """Найти контакт по имени или информации"""
    try:
        if not context.args:
            await update.message.reply_text("❌ Используйте: /find_contact <поисковый запрос>")
            return
        
        search_query = " ".join(context.args).lower()
        contacts = db.search_contacts(search_query)  # Новый метод
        
        if contacts:
            message = f"🔍 Найдено контактов: {len(contacts)}\n\n"
            for contact in contacts:
                message += f"👤 {contact['name']}\n"
                if contact['info']:
                    message += f"📝 {contact['info']}\n"
                message += "\n"
            await update.message.reply_text(message)
        else:
            await update.message.reply_text(f"❌ Контакты по запросу '{search_query}' не найдены.")
    except Exception as e:
        logger.error(f"Ошибка в find_contact: {e}")
        await update.message.reply_text("Произошла ошибка при поиске контакта.")
```

**Новый метод в Database:**

```python
def search_contacts(self, query: str) -> List[Dict]:
    """Найти контакты по части имени или информации"""
    try:
        conn = self.get_connection()
        cursor = conn.cursor()
        search_pattern = f'%{query}%'
        cursor.execute(
            '''SELECT name, info FROM contacts 
               WHERE name LIKE ? OR info LIKE ?
               ORDER BY name''',
            (search_pattern, search_pattern)
        )
        results = cursor.fetchall()
        conn.close()
        return [{'name': row['name'], 'info': row['info']} for row in results]
    except sqlite3.Error as e:
        logger.error(f"Ошибка поиска контактов: {e}")
        return []
```

#### Как это помогло

Теперь можно искать:
- По части имени: `/find_contact иван` найдет "Иван Иванов"
- По должности: `/find_contact разработчик` найдет всех разработчиков
- По email: `/find_contact @example.com` найдет контакты с этим доменом

#### Новый фидбек

**Алексей (Новый сотрудник):**
> "Отлично! Теперь намного проще найти нужного человека. Можно искать и по имени, и по должности. Это очень удобно!"

**Оценка:** 4.5/5 (было 3.5/5)

---

## Выводы

### Что получилось хорошо

1. **Успешный деплой на Amvera**:
   - Бот работает стабильно в облачной среде
   - Автоматический деплой из Git репозитория
   - Простое управление через веб-интерфейс
   - Размещение данных в России

2. **Быстрое решение проблем**:
   - Проблемы с версиями Python решены быстро
   - Настроена правильная работа с переменными окружения
   - Код адаптирован для облачной среды

3. **Реакция на фидбек**:
   - Улучшения внедрены быстро
   - Пользователи довольны изменениями
   - Оценки выросли после улучшений

4. **Улучшения функционала**:
   - Группировка событий по датам
   - Список всех вопросов
   - Улучшенный поиск контактов

### Что можно улучшить дальше

1. **Функциональность**:
   - Добавить редактирование и удаление записей (высокий приоритет)
   - Реализовать автоматические напоминания о событиях
   - Добавить экспорт данных в различные форматы

2. **UX/UI**:
   - Добавить inline-кнопки для навигации
   - Реализовать меню команд
   - Улучшить форматирование вывода

3. **Технические улучшения**:
   - Реализовать бэкап базы данных
   - Добавить мониторинг и аналитику использования
   - Оптимизировать производительность при большом количестве данных

4. **Интеграции**:
   - Интеграция с календарем (Google Calendar)
   - Синхронизация с CRM системами
   - API для интеграции с другими сервисами

### Чему научились

1. **Деплой на облачные платформы**:
   - Настройка переменных окружения
   - Работа с версиями Python и зависимостями
   - Мониторинг и логирование в облачной среде

2. **Работа с фидбеком**:
   - Сбор и анализ обратной связи от пользователей
   - Приоритизация улучшений
   - Итеративная разработка на основе фидбека

3. **Улучшение UX**:
   - Группировка и форматирование данных
   - Улучшение поиска и навигации
   - Адаптация под потребности пользователей

4. **Решение проблем**:
   - Диагностика проблем в облачной среде
   - Работа с версиями библиотек
   - Адаптация кода под разные среды выполнения

---

## Заключение

Деплой бота на Amvera прошел успешно, несмотря на несколько технических проблем, которые были быстро решены. Сбор и анализ фидбека от пользователей помог выявить ключевые направления для улучшения. Внедренные улучшения (группировка событий, список вопросов, улучшенный поиск) получили положительную оценку и повысили общую удовлетворенность пользователей.

Проект демонстрирует успешный цикл разработки: деплой → сбор фидбека → анализ → улучшения → новый фидбек. Это итеративный подход, который позволяет постоянно улучшать продукт на основе реальных потребностей пользователей.

Основные достижения:
- ✅ Успешный деплой на Amvera
- ✅ Решение всех технических проблем
- ✅ Внедрение улучшений на основе фидбека
- ✅ Повышение удовлетворенности пользователей
- ✅ Стабильная работа в облачной среде
- ✅ Размещение данных в России (соответствие ФЗ-152)

Проект готов к дальнейшему развитию и может быть расширен новыми функциями на основе потребностей команды.

