University: [ITMO University](https://itmo.ru/ru/)  
Faculty: [FICT](https://fict.itmo.ru)  
Course: [Vibe Coding: AI-боты для бизнеса](https://github.com/itmo-ict-faculty/vibe-coding-for-business)  
Year: 2025/2026  
Group: U4225  
Author: Shtof Fedor Danilovich  
Lab: Lab2  
Date of create: 09.11.2025  
Date of finished: 09.11.2025

# Отчет об интеграции: Telegram-бот помощник для команды

## Описание интеграции

Проект реализует интеграцию Telegram-бота с двумя основными источниками данных:

1. **Telegram Bot API** - источник входящих данных от пользователей (команды, сообщения)
2. **SQLite база данных** - хранилище данных бота (вопросы/ответы, контакты, события, дайджесты)

Бот работает как промежуточный слой между пользователями Telegram и локальной базой данных, обеспечивая удобный интерфейс для управления информацией команды.

---

## Какой источник данных выбрали

### Telegram Bot API

**Telegram Bot API** выбран как основной источник входящих данных по следующим причинам:

- **Удобство использования**: Telegram - популярный мессенджер, с которым пользователи уже знакомы
- **Богатый функционал**: API предоставляет множество возможностей (команды, диалоги, inline-кнопки)
- **Асинхронность**: Библиотека python-telegram-bot поддерживает асинхронную работу
- **Надежность**: Официальный API с хорошей документацией и поддержкой

### SQLite база данных

**SQLite** выбран как хранилище данных по следующим причинам:

- **Простота**: Не требует отдельного сервера, файловая база данных
- **Надежность**: ACID-совместимая транзакционная база данных
- **Производительность**: Достаточно для работы с небольшими объемами данных команды
- **Портативность**: Легко переносить и бэкапить (один файл)

### Почему именно этот

**Telegram Bot API:**
- ✅ Не требует разработки собственного интерфейса
- ✅ Кроссплатформенность (работает на всех устройствах)
- ✅ Мгновенные уведомления пользователям
- ✅ Бесплатный для базового использования
- ✅ Большое сообщество разработчиков

**SQLite:**
- ✅ Идеально для небольших проектов
- ✅ Не требует настройки и администрирования
- ✅ Встроена в Python (не нужны дополнительные зависимости)
- ✅ Легко интегрируется с приложением
- ✅ Достаточно для хранения данных команды (до нескольких тысяч записей)

---

## Структура данных

### База данных SQLite

База данных состоит из 4 основных таблиц:

#### 1. Таблица `company_info`
Хранит вопросы и ответы о компании/проекте/команде.

```sql
CREATE TABLE company_info (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    question TEXT UNIQUE,           -- Вопрос (в нижнем регистре для поиска)
    answer TEXT,                     -- Ответ на вопрос
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

**Структура данных:**
- `question` - текст вопроса (уникальный, используется как ключ поиска)
- `answer` - текст ответа
- `created_at` - время создания записи

#### 2. Таблица `contacts`
Хранит контактную информацию коллег.

```sql
CREATE TABLE contacts (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,              -- Имя коллеги
    info TEXT,                       -- Контактная информация (телефон, email, должность)
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

**Структура данных:**
- `name` - имя коллеги
- `info` - дополнительная информация (телефон, email, должность и т.д.)
- `created_at` - время создания записи

#### 3. Таблица `events`
Хранит события и напоминания.

```sql
CREATE TABLE events (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,              -- Название события
    event_date TIMESTAMP NOT NULL,   -- Дата и время события (ISO формат)
    description TEXT,                -- Описание события
    notified BOOLEAN DEFAULT 0,      -- Флаг отправки уведомления
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

**Структура данных:**
- `name` - название события
- `event_date` - дата и время в ISO формате (например: "2025-11-20T15:00:00")
- `description` - описание события
- `notified` - флаг, был ли отправлен reminder
- `created_at` - время создания записи

#### 4. Таблица `digests`
Хранит дайджесты команды.

```sql
CREATE TABLE digests (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    content TEXT NOT NULL,           -- Содержание дайджеста
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

**Структура данных:**
- `content` - текст дайджеста
- `created_at` - время создания записи

### Структура данных Telegram API

Бот получает данные через объект `Update`:

```python
Update {
    message: Message {
        text: str,              # Текст сообщения
        chat: Chat {
            id: int,            # ID чата
            type: str           # Тип чата (private, group)
        },
        from: User {
            id: int,            # ID пользователя
            username: str,      # Username
            first_name: str     # Имя
        }
    }
}
```

---

## Промпт для LLM

### Исходный промпт

```
Создай Telegram-бота на Python с использованием библиотеки python-telegram-bot.

Функционал бота:
- Отвечает на вопросы о компании/проекте/команде
- Хранит контакты коллег
- Напоминает о важных событиях
- Отправляет ежедневные дайджесты

Требования:
- Бот должен быть простым и понятным
- Код должен быть хорошо прокомментирован
- Использовать файл для хранения данных (SQLite)
- Добавить обработку ошибок

Создай:
1. Файл bot.py с кодом бота
2. Файл requirements.txt с зависимостями
3. Файл README.md с инструкцией по запуску
4. Файл .env.example для примера конфигурации
```

### Итерации (если были)

В процессе разработки и тестирования были внесены следующие улучшения:

1. **Улучшение парсинга вопросов/ответов**:
   - **Проблема**: Пользователи вводили "Вопрос:" и "Ответ:" с разным регистром
   - **Решение**: Использование регулярных выражений с `re.IGNORECASE`
   - **Промпт для улучшения**: "Исправь парсинг вопросов/ответов, чтобы он работал независимо от регистра"

2. **Исправление сохранения событий**:
   - **Проблема**: События не сохранялись корректно в БД
   - **Решение**: Сохранение дат в ISO формате
   - **Промпт для улучшения**: "Исправь сохранение дат событий, используй ISO формат для корректного сравнения"

3. **Расширение диапазона показа событий**:
   - **Проблема**: Показывались только события на 7 дней
   - **Решение**: Показ всех будущих событий
   - **Промпт для улучшения**: "Измени функцию показа событий, чтобы показывать все будущие события, а не только на 7 дней"

### Финальный промпт

После итераций основной промпт остался без изменений, так как он был достаточно детальным. Дополнительные промпты использовались для исправления конкретных багов, обнаруженных при тестировании.

---

## Реализация

### Как работает интеграция

#### 1. Инициализация бота

```python
# Получение токена из переменных окружения
token = os.getenv('TELEGRAM_BOT_TOKEN')

# Создание приложения
application = Application.builder().token(token).build()
```

Бот подключается к Telegram API используя токен, полученный от @BotFather.

#### 2. Обработка команд

Бот использует два типа обработчиков:

**CommandHandler** - для простых команд:
```python
application.add_handler(CommandHandler("start", start))
application.add_handler(CommandHandler("help", help_command))
```

**ConversationHandler** - для многошаговых диалогов:
```python
question_conv = ConversationHandler(
    entry_points=[CommandHandler("question", question_start)],
    states={
        ASKING_QUESTION: [MessageHandler(filters.TEXT & ~filters.COMMAND, question_received)]
    },
    fallbacks=[CommandHandler("cancel", cancel)]
)
```

#### 3. Работа с базой данных

Все операции с БД выполняются через класс `Database`:

```python
class Database:
    def __init__(self, db_name: str = 'bot_data.db'):
        self.db_name = db_name
        self.init_db()  # Создание таблиц при инициализации
    
    def get_connection(self):
        """Получить соединение с базой данных"""
        conn = sqlite3.connect(self.db_name)
        conn.row_factory = sqlite3.Row  # Возвращает результаты как словари
        return conn
```

#### 4. Поток данных

```
Пользователь → Telegram → Bot API → Обработчик команды → Database → SQLite
                                                              ↓
Пользователь ← Telegram ← Bot API ← Форматированный ответ ← Результат запроса
```

### Ключевые фрагменты кода

#### 1. Инициализация базы данных

```python
def init_db(self):
    """Инициализация базы данных и создание таблиц"""
    conn = self.get_connection()
    cursor = conn.cursor()
    
    # Создание таблицы company_info
    cursor.execute('''
        CREATE TABLE IF NOT EXISTS company_info (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            question TEXT UNIQUE,
            answer TEXT,
            created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
        )
    ''')
    
    # ... создание остальных таблиц
    
    conn.commit()
    conn.close()
```

#### 2. Сохранение события с правильным форматом даты

```python
def add_event(self, name: str, event_date: datetime, description: str = ""):
    """Добавить событие/напоминание"""
    conn = self.get_connection()
    cursor = conn.cursor()
    # Сохраняем дату в ISO формате для правильного сравнения
    event_date_str = event_date.isoformat()
    cursor.execute(
        'INSERT INTO events (name, event_date, description) VALUES (?, ?, ?)',
        (name, event_date_str, description)
    )
    conn.commit()
    conn.close()
    return True
```

#### 3. Получение событий с правильным сравнением дат

```python
def get_upcoming_events(self, days: int = None) -> List[Dict]:
    """Получить предстоящие события"""
    now = datetime.now()
    now_str = now.isoformat()
    
    if days is not None:
        end_date = now + timedelta(days=days)
        end_date_str = end_date.isoformat()
        cursor.execute(
            '''SELECT name, event_date, description FROM events 
               WHERE event_date >= ? AND event_date <= ? AND notified = 0
               ORDER BY event_date''',
            (now_str, end_date_str)
        )
    else:
        # Получить все будущие события
        cursor.execute(
            '''SELECT name, event_date, description FROM events 
               WHERE event_date >= ? AND notified = 0
               ORDER BY event_date''',
            (now_str,)
        )
    
    # Парсинг результатов
    events = []
    for row in results:
        event_date = datetime.fromisoformat(row['event_date'])
        events.append({
            'name': row['name'],
            'event_date': event_date,
            'description': row['description'] or ''
        })
    return events
```

#### 4. Парсинг вопросов/ответов с использованием регулярных выражений

```python
async def answer_received(update: Update, context: CallbackContext) -> int:
    """Обработать полученный ответ"""
    text = update.message.text
    text_lower = text.lower()
    
    if "вопрос" in text_lower and "ответ" in text_lower:
        # Разделяем по "ответ:" независимо от регистра
        parts = re.split(r'ответ:\s*', text, flags=re.IGNORECASE, maxsplit=1)
        
        if len(parts) == 2:
            # Извлекаем вопрос (убираем "вопрос:" в начале)
            question_part = parts[0]
            question = re.sub(r'^.*?вопрос:\s*', '', question_part, flags=re.IGNORECASE).strip()
            answer = parts[1].strip()
            
            if question and answer:
                db.add_company_info(question, answer)
```

#### 5. ConversationHandler для многошаговых диалогов

```python
# ConversationHandler для добавления событий
event_conv = ConversationHandler(
    entry_points=[CommandHandler("add_event", add_event_start)],
    states={
        ADDING_EVENT_NAME: [MessageHandler(filters.TEXT & ~filters.COMMAND, add_event_name)],
        ADDING_EVENT_DATE: [MessageHandler(filters.TEXT & ~filters.COMMAND, add_event_date)],
        ADDING_EVENT_DESCRIPTION: [MessageHandler(filters.TEXT & ~filters.COMMAND, add_event_description)]
    },
    fallbacks=[CommandHandler("cancel", cancel)]
)
application.add_handler(event_conv)
```

### Используемые библиотеки

1. **python-telegram-bot (v20.7)**
   - `Application` - основной класс приложения
   - `CommandHandler` - обработка команд
   - `MessageHandler` - обработка текстовых сообщений
   - `ConversationHandler` - многошаговые диалоги
   - `Update` - объект обновления от Telegram
   - `CallbackContext` - контекст обработчика

2. **sqlite3 (встроенная)**
   - `connect()` - подключение к базе данных
   - `cursor()` - выполнение SQL-запросов
   - `execute()` - выполнение запросов
   - `commit()` - сохранение изменений

3. **python-dotenv (v1.0.0)**
   - `load_dotenv()` - загрузка переменных окружения

4. **Стандартная библиотека Python**:
   - `re` - регулярные выражения для парсинга
   - `datetime` - работа с датами и временем
   - `logging` - логирование событий
   - `os` - работа с переменными окружения
   - `typing` - типизация для читаемости кода

---

## Тестирование

### Скриншоты работы

#### 1. Команда /start
Бот приветствует пользователя и показывает список доступных команд.

#### 2. Добавление вопроса/ответа
```
Пользователь: /answer
Бот: 📝 Введите вопрос и ответ в формате:
     Вопрос: ваш вопрос
     Ответ: ваш ответ

Пользователь: Вопрос: Где находится офис?
              Ответ: Офис находится по адресу: ул. Ломоносова, 9

Бот: ✅ Ответ успешно добавлен!
```

#### 3. Задание вопроса
```
Пользователь: /question
Бот: ❓ Задайте ваш вопрос о компании/проекте/команде:

Пользователь: Где находится офис?
Бот: 💡 Ответ:
     Офис находится по адресу: ул. Ломоносова, 9
```

#### 4. Добавление контакта
```
Пользователь: /add_contact
Бот: 👤 Введите имя коллеги:

Пользователь: Иван Иванов
Бот: 📝 Введите контактную информацию (телефон, email, должность и т.д.):

Пользователь: +7 (999) 123-45-67, ivan@example.com, Разработчик
Бот: ✅ Контакт Иван Иванов успешно добавлен!
```

#### 5. Добавление события
```
Пользователь: /add_event
Бот: 📅 Введите название события:

Пользователь: Планерка
Бот: 📆 Введите дату и время события в формате:
     ДД.ММ.ГГГГ ЧЧ:ММ
     Например: 25.12.2024 15:00

Пользователь: 20.11.2025 15:00
Бот: 📝 Введите описание события (или отправьте '-' чтобы пропустить):

Пользователь: Еженедельная планерка команды
Бот: ✅ Событие 'Планерка' успешно добавлено!
```

#### 6. Просмотр событий
```
Пользователь: /events
Бот: 📅 Предстоящие события:

     📌 Планерка
        📆 20.11.2025 15:00
        📝 Еженедельная планерка команды
```

### Примеры запросов и ответов

#### Пример 1: Работа с вопросами/ответами

**Запрос:**
```
/answer
Вопрос: Какие технологии использует команда?
Ответ: Команда использует Python, JavaScript, PostgreSQL, Docker
```

**Ответ бота:**
```
✅ Ответ успешно добавлен!
```

**Запрос:**
```
/question
Какие технологии использует команда?
```

**Ответ бота:**
```
💡 Ответ:
Команда использует Python, JavaScript, PostgreSQL, Docker
```

#### Пример 2: Работа с контактами

**Запрос:**
```
/add_contact
[после ввода имени и информации]
```

**Ответ бота:**
```
✅ Контакт Мария Петрова успешно добавлен!
```

**Запрос:**
```
/contacts
```

**Ответ бота:**
```
👥 Контакты коллег:

👤 Иван Иванов
   +7 (999) 123-45-67, ivan@example.com, Разработчик

👤 Мария Петрова
   +7 (999) 234-56-78, maria@example.com, Тестировщик
```

#### Пример 3: Работа с событиями

**Запрос:**
```
/events
```

**Ответ бота:**
```
📅 Предстоящие события:

📌 Планерка
   📆 20.11.2025 15:00
   📝 Еженедельная планерка команды

📌 Дедлайн проекта
   📆 25.12.2025 23:59
   📝 Финальный дедлайн проекта X
```

### Видео-демо

*Здесь можно добавить ссылку на видео-демонстрацию работы интеграции (YouTube/Google Drive)*

---

## Трудности и решения

### С какими проблемами столкнулись

#### 1. Проблема с парсингом вопросов/ответов

**Описание проблемы:**
Пользователи вводили "Вопрос:" и "Ответ:" с разным регистром (например, "ВОПРОС:" или "вопрос:"), и бот выдавал ошибку "неверный формат".

**Причина:**
Использование простого метода `split("ответ:")` было чувствительно к регистру. Также не учитывались возможные пробелы после двоеточия.

**Решение:**
- Использование регулярных выражений с флагом `re.IGNORECASE` для регистронезависимого поиска
- Использование паттерна `r'ответ:\s*'` для учета пробелов после двоеточия
- Правильное извлечение вопроса с помощью `re.sub()` для удаления префикса "вопрос:"

**Код решения:**
```python
import re

parts = re.split(r'ответ:\s*', text, flags=re.IGNORECASE, maxsplit=1)
question = re.sub(r'^.*?вопрос:\s*', '', question_part, flags=re.IGNORECASE).strip()
```

#### 2. События не сохранялись в БД

**Описание проблемы:**
После добавления события через `/add_event`, команда `/events` не показывала добавленные события, хотя бот сообщал об успешном добавлении.

**Причина:**
Объекты `datetime` сохранялись напрямую в SQLite, что приводило к проблемам при сравнении дат в SQL-запросах. SQLite хранит даты как строки, и прямое сравнение объектов datetime с строками не работало корректно.

**Решение:**
- Сохранение дат в ISO формате (строки) с помощью `datetime.isoformat()`
- Преобразование текущей даты в ISO формат перед сравнением в SQL-запросах
- Использование строкового сравнения в SQL: `WHERE event_date >= ?`

**Код решения:**
```python
# Сохранение
event_date_str = event_date.isoformat()
cursor.execute('INSERT INTO events (name, event_date, description) VALUES (?, ?, ?)',
               (name, event_date_str, description))

# Получение
now_str = datetime.now().isoformat()
cursor.execute('SELECT ... WHERE event_date >= ?', (now_str,))
```

#### 3. Ограничение показа событий

**Описание проблемы:**
Показывались только события на 7 дней вперед, дальние события (например, через месяц) не отображались.

**Причина:**
Жестко заданный параметр `days=7` в функции `get_upcoming_events()`.

**Решение:**
- Добавление параметра `days=None` для показа всех будущих событий
- Изменение SQL-запроса: при `days=None` убрать верхнюю границу даты
- Обновление команды `/events` для использования `days=None`

**Код решения:**
```python
def get_upcoming_events(self, days: int = None) -> List[Dict]:
    now_str = datetime.now().isoformat()
    
    if days is not None:
        end_date_str = (datetime.now() + timedelta(days=days)).isoformat()
        cursor.execute('WHERE event_date >= ? AND event_date <= ?', (now_str, end_date_str))
    else:
        cursor.execute('WHERE event_date >= ?', (now_str,))
```

#### 4. Обработка ошибок парсинга дат

**Описание проблемы:**
При чтении старых записей из БД (если они были сохранены в неправильном формате) могли возникать ошибки парсинга дат.

**Решение:**
- Добавлена обработка исключений при парсинге дат
- Логирование ошибок для отладки
- Пропуск некорректных записей с продолжением обработки остальных

**Код решения:**
```python
events = []
for row in results:
    try:
        event_date = datetime.fromisoformat(row['event_date'])
        events.append({...})
    except (ValueError, TypeError) as e:
        logger.error(f"Ошибка парсинга даты события: {e}, дата: {row['event_date']}")
        continue
```

### Как их решили

**Методология решения проблем:**

1. **Итеративный подход**: Тестирование каждой функции после реализации
2. **Логирование**: Добавление подробных логов для отладки (`logger.info()`, `logger.error()`)
3. **Обработка исключений**: Try-except блоки на всех критических участках кода
4. **Валидация данных**: Проверка формата данных перед сохранением
5. **Тестирование в реальных условиях**: Использование бота для выявления проблем

---

## Выводы

### Что получилось хорошо

1. **Надежная интеграция с Telegram API**:
   - Корректная работа всех команд
   - Удобные многошаговые диалоги через ConversationHandler
   - Обработка ошибок на всех уровнях

2. **Эффективная работа с базой данных**:
   - Правильная структура таблиц
   - Корректное сохранение и извлечение данных
   - Использование транзакций для целостности данных
   - Правильная работа с датами в ISO формате

3. **Удобный пользовательский интерфейс**:
   - Интуитивно понятные команды
   - Пошаговые диалоги для сложных операций
   - Понятные сообщения об ошибках
   - Возможность отмены операций

4. **Качество кода**:
   - Хорошая структура (класс Database, отдельные обработчики)
   - Подробные комментарии
   - Обработка ошибок
   - Логирование для отладки

5. **Гибкость парсинга**:
   - Регистронезависимый парсинг вопросов/ответов
   - Учет различных форматов ввода

### Что можно улучшить

1. **Функциональность**:
   - Добавить возможность редактирования и удаления записей
   - Реализовать поиск по частичному совпадению (не только точное)
   - Добавить категории/теги для событий и контактов
   - Реализовать автоматические напоминания о событиях

2. **Интеграция с внешними сервисами**:
   - Интеграция с календарем (Google Calendar, Outlook)
   - Синхронизация контактов с CRM системами
   - Экспорт данных в различные форматы (JSON, CSV, Excel)

3. **UX/UI улучшения**:
   - Добавить inline-кнопки для навигации
   - Реализовать меню команд
   - Улучшить форматирование вывода (markdown, HTML)
   - Добавить подтверждение при удалении

4. **Производительность и масштабируемость**:
   - Добавить индексы в БД для ускорения поиска
   - Реализовать кэширование часто запрашиваемых данных
   - Оптимизировать SQL-запросы
   - Рассмотреть миграцию на PostgreSQL для больших объемов данных

5. **Безопасность**:
   - Добавить авторизацию пользователей (whitelist)
   - Ограничить доступ к некоторым командам
   - Валидация и санитизация всех входных данных
   - Защита от SQL-инъекций (уже реализовано через параметризованные запросы)

6. **Тестирование**:
   - Добавить unit-тесты для всех функций
   - Интеграционные тесты для проверки работы с БД
   - Тесты для обработчиков команд

7. **Мониторинг и аналитика**:
   - Добавить метрики использования команд
   - Логирование действий пользователей
   - Статистика использования бота

8. **Документация**:
   - API документация для функций Database
   - Диаграммы потоков данных
   - Примеры использования

---

## Заключение

Интеграция Telegram-бота с SQLite базой данных успешно реализована и работает стабильно. Бот обеспечивает удобный интерфейс для управления информацией команды, используя Telegram как канал связи и SQLite как надежное хранилище данных.

Основные достижения:
- ✅ Надежная интеграция с Telegram Bot API
- ✅ Эффективная работа с SQLite базой данных
- ✅ Корректная обработка всех типов данных (текст, даты)
- ✅ Удобный пользовательский интерфейс
- ✅ Обработка ошибок на всех уровнях

Проект демонстрирует успешную интеграцию двух различных систем (Telegram API и SQLite) для создания полезного инструмента для команды.

